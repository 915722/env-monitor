# 🚀 GitHub Pages 快速部署指南

## 第一步：修改仓库名配置

打开 `vite.config.ts`，将第 8 行的 `/env-monitor/` 改为你的仓库名：

```typescript
base: process.env.NODE_ENV === 'production' ? '/你的仓库名/' : '/',
```

**示例**：
- 仓库地址是 `https://github.com/zhangsan/my-project`
- 则改为：`'/my-project/'`

---

## 第二步：推送代码到 GitHub

### 1. 在 GitHub 创建新仓库

1. 访问 https://github.com/new
2. 填写仓库名（例如：`env-monitor`）
3. 选择 Public（公开）
4. **不要**勾选 "Add a README file"
5. 点击 "Create repository"

### 2. 推送本地代码

在项目目录打开终端，执行：

```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 关联远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

---

## 第三步：配置 GitHub Pages

1. 打开你的 GitHub 仓库页面
2. 点击 **Settings**（设置）
3. 左侧菜单找到 **Pages**
4. 在 **Build and deployment** 部分：
   - **Source**: 选择 **GitHub Actions**
5. 保存

---

## 第四步：等待部署完成

1. 回到仓库主页
2. 点击顶部的 **Actions** 标签
3. 查看 "Deploy to GitHub Pages" 工作流
4. 等待绿色的 ✓ 出现（约 2-5 分钟）

---

## 第五步：访问你的网站

部署成功后，访问：

```
https://你的用户名.github.io/你的仓库名/
```

**示例**：
- 用户名：`zhangsan`
- 仓库名：`env-monitor`
- 访问地址：`https://zhangsan.github.io/env-monitor/`

---

## 更新网站

修改代码后，只需推送到 GitHub：

```bash
git add .
git commit -m "Update"
git push
```

GitHub Actions 会自动构建和部署。

---

## 常见问题

### 1. 404 页面找不到

**原因**：`base` 配置错误

**解决**：
1. 检查 `vite.config.ts` 第 8 行的仓库名是否正确
2. 确保格式为：`'/仓库名/'`（前后都有斜杠）
3. 重新推送：
   ```bash
   git add vite.config.ts
   git commit -m "Fix base path"
   git push
   ```

### 2. 构建失败

**查看错误**：
1. 进入仓库的 **Actions** 标签
2. 点击失败的工作流
3. 查看红色 ✗ 的步骤详情

**常见原因**：
- 依赖安装失败：删除 `package-lock.json`，重新提交
- 内存不足：配置文件已处理
- Node 版本：配置文件已使用 Node 18

### 3. 样式或图片加载失败

**原因**：资源路径问题

**解决**：
- 确认 `vite.config.ts` 中 `base` 正确
- 确认图片等资源放在 `public/` 目录或通过 `import` 引入

---

## 完整配置文件

项目已包含：

- ✅ `.github/workflows/deploy.yml` - GitHub Actions 配置
- ✅ `vite.config.ts` - 已配置 base 路径（需修改）
- ✅ `package.json` - 构建脚本

你只需要：
1. 修改 `vite.config.ts` 中的仓库名
2. 推送代码到 GitHub
3. 配置 GitHub Pages

---

## 需要帮助？

详细教程请查看：[docs/GITHUB_PAGES.md](docs/GITHUB_PAGES.md)

---

**祝部署顺利！** 🎉

